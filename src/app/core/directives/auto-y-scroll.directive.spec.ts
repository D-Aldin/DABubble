import { AutoYScrollDirective } from './auto-y-scroll.directive';

describe('AutoYScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoYScrollDirective();
    expect(directive).toBeTruthy();
  });
});
